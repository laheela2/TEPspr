<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">

<select id="loginCheck" parameterType="com.tep.members.model.MembersModel" resultType="com.tep.members.model.MembersModel">
       <![CDATA[
            SELECT
               	m_no, m_email, m_name, m_nickname, m_password, m_company, m_phone, m_fav_area,	m_fav_field, m_date
        	 FROM
                members
             WHERE 
             	m_email = #{m_email} and m_password=#{m_password}
            ORDER BY 
            	m_no desc
        ]]>
	</select>

<select id="findId" parameterType="com.tep.members.model.MembersModel" resultType="com.tep.members.model.MembersModel">	
		       <![CDATA[
            SELECT
               	m_no, m_email, m_name, m_nickname, m_password, m_company, m_phone, m_fav_area,	m_fav_field, m_date
        	 FROM
                members
             WHERE 
             	m_name = #{m_name} and m_phone=#{m_phone}
            ORDER BY 
            	m_no desc
        ]]>
      </select>
</mapper>